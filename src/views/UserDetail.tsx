/** @format */

import { Stack } from "@mui/material";
import { useParams } from "react-router-dom";

const UserDetail = () => {
  const { id } = useParams();

  return <Stack></Stack>;
};

export default UserDetail;
